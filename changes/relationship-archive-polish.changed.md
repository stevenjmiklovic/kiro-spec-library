Refined the Relationship view: empty status columns collapse to thin rails to reclaim horizontal space, each spec card carries a tinted per-stage pill so its column and status label agree, relationship edge labels no longer truncate and show a direction affordance, the X/Y axis controls are grouped into a labeled "View" cluster distinct from the filters, and the header actions are grouped by purpose (manage / data / app). The Archive ledger gains a matching type pill and the same grouped header chrome.
